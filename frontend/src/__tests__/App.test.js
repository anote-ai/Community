// Smoke test — confirms the React app module loads without errors.
// Add component-level tests here as the app grows.
import React from 'react';

test('React is available', () => {
  expect(React).toBeDefined();
});
